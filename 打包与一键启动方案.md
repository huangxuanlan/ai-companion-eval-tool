# 长文模式工具打包与一键启动方案

## 现状

- 已移除运行时对 `E:` 盘绝对路径的强依赖，改为“环境变量优先 + 便携包目录 + 当前工作区同级目录”三层解析。
- 新增 `launcher.py`，首次启动会自动创建 `.runtime/venv` 并安装 `server/requirements.txt`。
- 新增 `start.bat` 和 `start.command`，分别给 Windows 和 macOS 双击启动。
- 新增 `scripts/build_portable_bundle.py`，可生成不带真实密钥的便携分发目录。

## 推荐交付形态

| 目标人群 | 推荐形态 | 原因 |
|---|---|---|
| Windows 同事 | 便携目录 + `start.bat` | 成本最低，今天就能交付 |
| macOS 领导 | 便携目录 + `start.command` | 双击即可启动，路径已兼容 |
| 后续正式发行 | Windows `exe` / macOS `.app` | 真正做到无 Python 前置依赖 |

## 便携包构建

在仓库根目录执行：

```powershell
python .\scripts\build_portable_bundle.py
```

输出目录：

```text
dist/longform-tool-portable/
```

这个目录会包含：

- Web 工具运行代码
- 前端静态资源
- prompt-validator-llm 的 providers 和模型配置
- promptfoo 打分脚本与长文模式评分配置
- 长文模式提示词、测试提示词、摘要提示词、打分提示词、变量
- `server/.env.example`
- Windows / macOS 启动脚本

## 分发要求

1. 不要分发 `server/.env` 真文件，只分发 `server/.env.example`。
2. 让接收方把自己的密钥填进 `server/.env`。
3. Windows 直接双击 `start.bat`。
4. macOS 首次执行前运行一次：

```bash
chmod +x start.command
```

然后双击 `start.command`。

## 原生安装包方案

### Windows

- 在 Windows 构建机上用 PyInstaller 打 `one-dir`。
- 入口文件用 `launcher.py`。
- 把 `bundle_assets/` 一起打进发布目录。

示例命令：

```powershell
pyinstaller --noconfirm --onedir --name longform-tool launcher.py
```

### macOS

- 必须在 macOS 上构建，不能在 Windows 上交叉产出 `.app`。
- 同样以 `launcher.py` 为入口，用 PyInstaller 或 Briefcase。
- 把 `bundle_assets/` 放进 `.app/Contents/Resources/`，启动时通过环境变量指向资源目录。

## 当前边界

- 现在的一键启动依赖本机能运行 Python 3.11+。
- 如果目标是完全无 Python 前置依赖，下一步要加一条 macOS 构建链和一条 Windows 构建链。
- 评分和模型调用都依赖接收方自己的 API Key；这不是缺陷，这是必须保留的安全边界。
